// worker.js
const emailQueue = require('./emailQueue'); // Adjust the path if necessary

// This file doesn't need additional code since the email processing logic is already in emailQueue.js
console.log('Email worker is running...'); // Indicate that the worker is active


// to run this file "src/queues/worker.js"